#include "main.h"

/**
 * _puts - A functions that prints a string followed by a new line, to stdout
 *
 * @str: Pointer to the first character of the string.
 *
 * Return: Nothing(void...)
 */

void _puts(char *str)
{
	puts(str);
}
